name: Other Collector

on:
  schedule:
    # Analog Weekday-Cadence: alle 30 Minuten, Mo-Fr, 02:00-22:00 UTC
    - cron: '0,30 2-22 * * 1-5'
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: security-prices-write
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run collector
        run: python scripts/collect_other.py

      - name: Commit & push if changed
        run: |
          git config user.name "securitydatacollector-bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/security_prices.csv
          if git diff --cached --quiet; then
            echo "Keine Änderungen."
          else
            git commit -m "Other Collector: neue Kurse $(date -u +'%Y-%m-%d %H:%M UTC')"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push
          fi
